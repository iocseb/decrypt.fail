source "https://rubygems.org"

# GitHub Pages builds your site with this gem on its servers — you don't
# strictly need anything installed locally. Keep this only if you want to
# run `bundle exec jekyll serve` to preview before pushing.
gem "github-pages", group: :jekyll_plugins
